/**
 * Date Renderer
 */
const getDate = require("@versini/date-logic");

(() => {
  console.info("Date Renderer Header");
  console.log(`====> Date: [ ${getDate()} ]`);
  console.info("Date Renderer Footer");
})();
